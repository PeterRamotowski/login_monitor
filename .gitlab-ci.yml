include:
  - project: $_GITLAB_TEMPLATES_REPO
    ref: $_GITLAB_TEMPLATES_REF
    file:
      - '/includes/include.drupalci.main.yml'
      - '/includes/include.drupalci.variables.yml'
      - '/includes/include.drupalci.workflows.yml'

variables:
  _CSPELL_WORDS: 'piotr, ramotowski, anypassword, blockeduser, formtestuser, invaliduser, logoutuser, multiuser, nonexistentuser, onetimeuser, testuser, testuseronetime, validuser'